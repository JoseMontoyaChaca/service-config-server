server:
  port: 8081
  
spring:
  cloud:  
    config:
      server:
        git:
          uri: https://github.com/JoseMontoyaChaca/config-server.git
          search-paths: config-server 
  security:
    user:
      name: JoseMontoyaChaca
      password: Xavi4###

#spring:  
#  application:
#    name: eureka-server
#  cloud:
#    config:
#      failFast: true
#      uri: http://config-server:8888